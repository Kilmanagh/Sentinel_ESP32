# Sentinel ESP32 Project Reference

This file is kept only as a legacy pointer.

The old idea of a single "full project reference" is no longer useful because the active Sentinel project has moved to an ESPHome-centered documentation set and the previous PlatformIO and MQTT-oriented summary became misleading.

Use these documents instead:

- `README.md` for the current top-level project overview
- `docs/Adjustments.MD` for active ESPHome tuning and runtime controls
- `docs/Hardware_Deliverables.md` for current hardware and pin mapping
- `docs/Sentinel_Home_Assistant_Helpers.md` for Home Assistant helper entities and package behavior
- `docs/Sentinel_Lovelace_Dashboard.md` for dashboard setup and usage

If this file stops being referenced anywhere, it can be deleted.
